COMMENT    = _{ "#" ~ (!"\n" ~ ANY)* }
WHITESPACE = _{ " " }

quit   = !{ "quit" | "q" }
open   = !{ "open" | "o" }
errors = !{ "errors" }

verb = !{ quit | open | errors }

arg = !{ (!WHITESPACE ~ ANY)+ }

line = { SOI ~ ":" ~ verb ~ arg* ~ EOI }
