COMMENT    = _{ "#" ~ (!"\n" ~ ANY)* }
WHITESPACE = _{ " " }

quit   = !{ "quit" | "q" }
open   = !{ "open" | "o" }
errors = !{ "errors" }
info   = !{ "info" }

verb = !{ quit | open | errors | info }

arg = !{ (!WHITESPACE ~ ANY)+ }

line = { SOI ~ ":" ~ verb ~ arg* ~ EOI }
